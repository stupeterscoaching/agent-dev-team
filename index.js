const express = require('express'); const app = express(); const port = 3000; app.use(express.json()); app.get('/add', (req, res) => { res.send({ result: req.body.num1 + req.body.num2 }); }); app.get('/subtract', (req, res) => { res.send({ result: req.body.num1 - req.body.num2 }); }); app.get('/multiply', (req, res) => { res.send({ result: req.body.num1 * req.body.num2 }); }); app.get('/divide', (req, res) => { if(req.body.num2 !== 0) { res.send({ result: req.body.num1 / req.body.num2 }); } else { res.status(400).send({ error: 'Cannot divide by zero' }); } }); app.listen(port, () => { console.log(`Server started on port ${port}`); });